#include "Communication.h"

void Communication::begin()
{
    // Wi-Fi, Bluetooth, LoRa, or another interface
    // will be initialized here later.
}

void Communication::send(const SensorData& data)
{
    // Telemetry transmission will be added later.
    (void)data;
}